import { SearchFacPipe } from './search-fac.pipe';

describe('SearchFacPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFacPipe();
    expect(pipe).toBeTruthy();
  });
});
